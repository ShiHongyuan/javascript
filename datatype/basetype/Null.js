//Null的字面值只有一个：null
console.log(typeof(null));  //object  null可以说是一种特殊的原始数据类型，也可以说是一种特殊的对象类型

//定义的变量准备用于保存对象，还没有真正保存对象的，先初始化为null


//null
var car = null;
if(car != null){

}else{
    console.log(car); 
}

//true
console.log(undefined == null);